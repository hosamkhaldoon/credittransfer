# Test Pod for External Connectivity Testing
# Used by deploy scripts to test SQL Server and NoBill connectivity
# Generated by Cursor

apiVersion: v1
kind: Pod
metadata:
  name: nettest
  namespace: credittransfer
  labels:
    app: nettest
    component: test
spec:
  containers:
  - name: nettest
    image: busybox:1.35
    command: ['sleep', '3600']
    resources:
      requests:
        memory: "64Mi"
        cpu: "100m"
      limits:
        memory: "128Mi"
        cpu: "200m"
  restartPolicy: Always 